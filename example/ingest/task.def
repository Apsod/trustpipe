Bootstrap: docker
From: ubuntu:22.04

%files
    urls.txt /urls.txt

%post
    apt-get update
    apt-get install -y wget

%runscript
    wget --continue --no-verbose --directory-prefix=/output --input-file=/urls.txt
